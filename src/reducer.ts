import { Actions, ColourActions, COLOUR_ACTION_TYPES } from './actions';
import { State } from './state';

function colourReducer(state: State, { action }: ColourActions) {
  switch (action.type) {
    case 'setColor':
      break;
    case 'setValue':
  }
}
